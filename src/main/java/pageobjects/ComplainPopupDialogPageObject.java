package pageobjects;

import org.openqa.selenium.WebDriver;

public class ComplainPopupDialogPageObject extends BasePageObject {
    public ComplainPopupDialogPageObject(WebDriver driver) {
        super(driver);
    }
}
